# run_all.py chains the three CSV-producing stages (mappings -> catalog ->
# details). Running dailymed_downloader.py with a single subcommand published
# only the catalog CSV.
entrypoint: run_all.py
in_place: true
output_subdirs: [dailymed_data]
size_class: heavy
timeout_min: 360
mirror: true
enabled: true
