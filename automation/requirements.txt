# Pipeline + S3
boto3>=1.34
PyYAML>=6.0
apache-airflow-providers-amazon>=8.0
apache-airflow-providers-ssh>=3.10     # graph_sync drives the graph host over SSH

# Shared scraper runtime deps (union across the source scrapers)
requests>=2.28
beautifulsoup4>=4.11
lxml>=4.9
openpyxl>=3.0
pandas>=2.0
python-dateutil>=2.8

# Added for contributor scrapers (Clinical Trials / Drug Ref / Ontologies / Regulatory)
openai>=1.0            # MiniMax LLM client (ema)
pypdf>=4.0             # PDF text extraction (ema, mhra, pmda)
curl_cffi>=0.6         # TLS-fingerprint HTTP (jrct)
tqdm>=4.66             # progress bars
python-dotenv>=1.0     # .env loading
numpy>=1.26            # (transitive via pandas; pinned explicitly)
playwright>=1.44       # headless browser (anzctr, chictr, isrctn, loinc) — see Dockerfile
